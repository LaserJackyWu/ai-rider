name: Finalize Taiwan-first 100-channel IPTV list

on:
  push:
    branches: [tubo-playlist]
    paths:
      - tubo/finalize_taiwan_100.py
      - .github/workflows/finalize-tubo-100.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout playlist branch
        uses: actions/checkout@v4
        with:
          ref: tubo-playlist

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependency
        run: python -m pip install --disable-pip-version-check requests

      - name: Verify replacements and finalize playlist
        run: python tubo/finalize_taiwan_100.py

      - name: Commit finalized playlist and report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tubo/taiwan_100_50pct.m3u tubo/taiwan_100_50pct_report.json
          if ! git diff --cached --quiet; then
            git commit -m "Finalize public 100-channel IPTV playlist"
            git push origin HEAD:tubo-playlist
          fi
